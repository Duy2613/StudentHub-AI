# StudentHub AI — Live Provider Evidence

**Date:** 2026-09-09  
**Status:** `COST_ESTIMATE_ONLY; LIVE_PROVIDER_EVIDENCE_NOT_ESTABLISHED`

No approved live provider response, verified price sheet snapshot, observed token ledger, billing export or production p95 sample was available in this run. The gateway failure matrix is local mock-provider evidence: 5/5 failure scenarios pass, including bounded retry/fallback and truthful all-down abstention.

The FAST/NORMAL/DEEP cost numbers are modelled from assumed token counts and unit prices. The latency numbers are deterministic synthetic samples, not live p95 telemetry. They must not be described as measured provider cost or latency.

Required next evidence: provider/model ID, UTC timestamp, request/response status, observed prompt and completion tokens, retry/fallback trace, authoritative price source/date, and an approved cost calculation. Until then, retain `COST_ESTIMATE_ONLY`.

