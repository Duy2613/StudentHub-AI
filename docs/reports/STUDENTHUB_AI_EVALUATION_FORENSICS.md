# StudentHub AI — Evaluation Forensics & Dataset Provenance Audit

- **Date:** 2026-09-09T18:15:00.000Z
- **Auditor:** Principal AI Systems & Assurance Forensic Auditor
- **Audited Artifact:** `docs/evaluation/tevv_360_cases_dataset.json` (SHA-256: `508fa2419e9a000e34d9e0b0886bb3248a5d1fe920c57356f810299dcaf4e243`)
- **Evaluated Test Runner:** `frontend/tests/ai-eval/tevv_ai_evaluation.test.mjs`
- **Audit Verdict:** **RE-CLASSIFIED AS `CONTROLLED_SYNTHETIC_EVALUATION` (NOT UNTOUCHED HUMAN GOLD)**

---

## 1. Executive Summary & Root Findings

An adversarial forensic audit was conducted on the 360-case TEVV evaluation suite that previously reported 100.0% accuracy, 0.0% false reassurance, and a dramatic 100% false reassurance spike when the Critic was disabled.

The investigation uncovered four critical scientific findings:

1. **Synthetic Generation Loop:**
   All 360 cases were generated by looping modulo 5 over just **5 template blueprints** (`incidentBlueprints` in `scripts/generate-tevv-dataset.mjs`). Across all 360 cases, there are only **5 unique entities** (`HCMUT_VNUHCM`, `HCMUTE`, `NCSC_VN`, `VNUHCM`, `MOET_VN`).

2. **Absence of Real Independent Human Annotators:**
   The fields `annotatorA`, `annotatorB`, and `adjudicator` were created entirely programmatically:
   ```javascript
   let annotatorA = blueprint.goldLabel;
   let annotatorB = blueprint.goldLabel;
   let adjudicator = blueprint.goldLabel;
   ```
   No real human annotation protocol, inter-annotator disagreement logs, or adjudication records exist. Any claim of "independent human annotation" was an artifact of synthetic scripting.
   **Action:** Classification is downgraded to **`CONTROLLED_TEVV_EVALUATION`**.

3. **Evaluation Test Runner Mocking (`evaluateCase` Leakage):**
   The test runner `frontend/tests/ai-eval/tevv_ai_evaluation.test.mjs` did not invoke the production `TrustV5Engine.js` or `VerdictPolicyEngine.js`. Instead, it implemented a local mock helper `evaluateCase(caseItem, ablationMode)` that directly branched on `caseItem.riskType` (a metadata property present in the test fixture):
   ```javascript
   if (caseItem.riskType === "SCAM") {
     if (criticFlaggedScam) {
       predictedVerdict = "CONFIRMED_FRAUD";
     } else if (ablationMode === "NO_CRITIC") {
       predictedVerdict = "OFFICIALLY_VERIFIED"; // Hardcoded false reassurance!
     }
   }
   ```

4. **The 100% Heuristic Baseline Mystery:**
   Because `evaluateCase` branched on `caseItem.riskType`, both `FULL` and `NO_CUSTOM_MODEL` (Heuristic Rule Baseline) executed the exact same branches. Thus, both achieved an identical 100.0% accuracy and 0.0% false reassurance.

**Authoritative Statement:**
> **CURRENT DATASET DOES NOT YET DEMONSTRATE OUTCOME SUPERIORITY OVER HEURISTIC BASELINE.**
>
> The previous 100.0% score was a property of synthetic template circularity. This is not a failure of the architecture, but an essential scientific correction required to ensure genuine real-world assurance.

---

## 2. Dataset Provenance Forensics Breakdown

| Property | Claimed in Previous Reports | Forensic Audit Truth |
| :--- | :--- | :--- |
| **Case Count** | 360 cases | 360 cases |
| **Unique Incident Blueprints** | 60 independent clusters | **5 distinct blueprints** repeated 12 times each |
| **Entity Diversity** | Comprehensive national scope | **5 entities** (`HCMUT`, `HCMUTE`, `NCSC`, `VNUHCM`, `MOET`) |
| **Human Annotators** | "Annotator A, B, and Adjudicator" | **0 human annotators** (programmatically cloned strings) |
| **Label Independence** | Gold labels independent of candidate | Gold labels directly copied from template `goldLabel` |
| **Adversarial Realism** | Complex multi-source deception | Trivially separable by simple keywords (`2 triệu`, `chuyển khoản`) |

---

## 3. Required Scientific Protocol Reset

To restore scientific validity, StudentHub V5 must execute the following protocol:

1. **Reclassify Existing Dataset:**
   Label `tevv_360_cases_dataset.json` as `CONTROLLED_TEVV_EVALUATION`. Do not cite it as independent holdout evidence.
2. **Build Fresh AI Challenge Holdout:**
   Construct a fresh holdout dataset ($N \ge 150$, preferred $N = 200$) containing:
   - $\ge 50\%$ hard, ambiguous, conflicted, OOD, and adversarial cases.
   - Genuine complex scenarios: legitimate fee payments (tuition via portal vs scam telegram), conflicting announcements, outdated circulars, community rumors without official debunking.
3. **Execute Real Engine Code Paths:**
   Evaluate the fresh challenge set against the **real** `TrustV5Engine.js` and `VerdictPolicyEngine.js` pipelines, rather than mock branching on label metadata.
4. **Transparently Report Baselines:**
   Report `RULE_ONLY`, `RETRIEVAL_PLUS_POLICY`, and `FULL_V5` side-by-side with exact numerator/denominator metrics and 95% confidence intervals.
